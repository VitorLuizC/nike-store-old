.banner
  background-color: $banner.color
  & > .container
    & > .image
      centralize(block)
      display: block

#just-do-it
  background: #000

  & > .container
    overflow: hidden;
    height: 35.4375rem
    & > .image
      width: 100%
      opacity: .6
